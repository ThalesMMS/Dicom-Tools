# JS Tasks (Cornerstone/Viewer)
- Instalar pacotes publicados (`@cornerstonejs/core`, `@cornerstonejs/tools`, `@cornerstonejs/dicom-image-loader`) em um app simples (Vite/webpack) e validar render de `sample_series` servida via HTTP.
- Esboçar gateway JS que converta paths/outputs do contrato em `imageIds` WADO-URI e configure `RenderingEngine` + viewports.
- Adicionar testes rápidos (Jest ou vitest) para o gateway (ex.: mapeamento de imageIds, inicialização de viewport).
